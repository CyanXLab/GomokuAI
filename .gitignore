# Dependencies
node_modules/

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp
*.swo
Thumbs.db

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Build intermediate (keep dist/ and public/build/ as they contain the compiled WASM engine)
# Note: dist/ and public/build/ are intentionally committed so the repo is deployable as-is.
